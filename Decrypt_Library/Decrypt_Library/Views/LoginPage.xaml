<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:decrypt_library="clr-namespace:Decrypt_Library" x:DataType="decrypt_library:MainPage"
             x:Class="Decrypt_Library.Views.LoginPage">
    <ContentPage.Content>
        <StackLayout>
            <Label x:Name="Headline" Text="Logga in" />
            
            <Entry x:Name="SSN" Placeholder="Personnummer" />
            <Entry x:Name="Password" Placeholder="Lösenord" IsPassword="True" />
            <Button x:Name="LogIn" Text="Logga in" Clicked="LogIn_Clicked" BackgroundColor="Black" TextColor="White"/>
            <Label x:Name="Error" Text="Felaktigt personnummer eller lösenord. Försök igen!" TextColor="Red" HorizontalOptions="Center" IsVisible="false" />
            <Label x:Name="Test" Text="" />
            <Button x:Name="LogOut" Text="Logga ut" Clicked="LogOut_Clicked" IsVisible="false" BackgroundColor="Black" TextColor="White" />

        </StackLayout>
    </ContentPage.Content>
</ContentPage>