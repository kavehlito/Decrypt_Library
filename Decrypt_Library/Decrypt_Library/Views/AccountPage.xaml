<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="Decrypt_Library.Views.AccountPage">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>

            </Grid.ColumnDefinitions>

            <StackLayout>

                <StackLayout Orientation="Horizontal" >

                    <Button x:Name="MyProfile" Text="Min profil" Clicked="Button_Clicked" />
                    <Button x:Name="MyLoan" Text="Mina lån" Clicked="Button_Clicked_1" />
                    <Button x:Name="MyReservation" Text="Mina reservationer" Clicked="Button_Clicked_2" />
                    <Button x:Name="MyLoanHistory" Text="Lånehistorik" Clicked="MyLoanHistory_Clicked"/>

                </StackLayout>
                
                <Frame BackgroundColor="Black" 
                   Grid.Column="1"
                   HeightRequest="400"
                   CornerRadius="10"
                   BorderColor="White"
                   WidthRequest="500"
                   Grid.Row="1">

                    <StackLayout>

                        <ListView x:Name="reservations" IsVisible="true">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <ViewCell>
                                        <StackLayout Orientation="Horizontal" HorizontalOptions="StartAndExpand">
                                            <StackLayout Orientation="Vertical" HorizontalOptions="Start">

                                                <Label Text="{Binding Title}" TextColor="White"/>
                                                <Label Text="{Binding Author}" TextColor="White"/>
                                                <Label Text="{Binding ISBN}" TextColor="White"/>
                                                <Label Text="{Binding StartDate}" TextColor="White"/>
                                                <Label Text="{Binding EndDate}" TextColor="White"/>
                                            </StackLayout>
                                            <StackLayout Orientation="Horizontal" HorizontalOptions="End">
                                                <Button x:Name="removeButton" Text="Radera" IsVisible="true" Clicked="removeButton_Clicked"
                                                    BackgroundColor="Black" TextColor="White" FontAttributes="Bold"
                                                    CornerRadius="10" BorderColor="White" HorizontalOptions="End">
                                                </Button>
                                            </StackLayout>
                                        </StackLayout>
                                    </ViewCell>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                        <StackLayout>
                            <StackLayout>

                                <ListView x:Name="loanList" IsVisible="true">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <ViewCell>
                                                <StackLayout Orientation="Horizontal" HorizontalOptions="StartAndExpand">
                                                    <StackLayout Orientation="Vertical" HorizontalOptions="Start">

                                                        <Label Text="{Binding Title}" TextColor="White"/>
                                                        <Label Text="{Binding Author}" TextColor="White"/>
                                                        <Label Text="{Binding ISBN}" TextColor="White"/>
                                                        <Label Text="{Binding StartDate}" TextColor="White"/>
                                                        <Label Text="{Binding EndDate}" TextColor="White"/>
                                                    </StackLayout>
                                                </StackLayout>
                                            </ViewCell>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </StackLayout>
                        </StackLayout>
                    </StackLayout>
                </Frame>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>